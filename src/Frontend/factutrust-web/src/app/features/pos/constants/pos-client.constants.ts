/**
 * Constants for the default "client passager" (walk-in client).
 * Aligned with backend DefaultPassengerClient for POS validation rules.
 */
export const POS_PASSENGER_CLIENT_EMAIL = 'passager@factutrust.local';
export const POS_PASSENGER_CLIENT_NAME = 'Client passager';
